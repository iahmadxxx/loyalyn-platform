'use client'
import {LayoutDashboard,Users,Bell,WalletCards,LogOut} from 'lucide-react'
const items=[['overview','نظرة عامة',LayoutDashboard],['customers','العملاء والنقاط',Users],['notifications','الإشعارات',Bell],['wallet','Apple Wallet',WalletCards]] as const
export function Shell({children,active,onChange}:{children:React.ReactNode,active:string,onChange:(x:string)=>void}){return <div className="min-h-screen grid-bg md:flex"><aside className="hidden md:flex w-72 p-5 border-l border-white/10 flex-col sticky top-0 h-screen"><div className="text-2xl font-black">LOYALYN<span className="text-lime-300">.</span></div><p className="text-xs text-white/35 mt-1">LOYALTY, BUILT TO GROW</p><nav className="space-y-2 mt-8">{items.map(([id,l,I])=><button key={id} onClick={()=>onChange(id)} className={`nav-btn ${active===id?'active':''}`}><I size={18}/>{l}</button>)}</nav><button onClick={()=>{localStorage.removeItem('loyalyn_token');location.href='/login'}} className="nav-btn mt-auto"><LogOut size={18}/>تسجيل الخروج</button></aside><main className="flex-1 p-4 md:p-8 pb-24">{children}</main><nav className="md:hidden fixed bottom-0 inset-x-0 glass p-2 flex justify-around z-50">{items.map(([id,l,I])=><button key={id} onClick={()=>onChange(id)} className={`mobile-nav ${active===id?'text-lime-300':'text-white/50'}`}><I size={20}/>{l}</button>)}</nav></div>}
